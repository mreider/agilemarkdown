# Agile Markdown coach mode

The coach stance lives in `.claude/agilemarkdown-coach.md`. Claude Code reads imported files when this CLAUDE.md references them with `@`.

@.claude/agilemarkdown-coach.md
